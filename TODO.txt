TODO: Optimize Context Ingestion, Token Density, and Multi-Type Parsing Handlers
---------------------------------------------------------------------------------
[ ] Implement Tool Output / Data Listing Truncation Layer in engine/parser.py:
    - Add regex/logic checks to detect automated XML tags (<tool_call>, <tool_response>) or deep text blocks dominated by file list patterns.
    - Truncate data dumps crossing a specific line threshold (e.g., max_lines = 15) to a tiny head/tail summary, isolating the model from token noise.
[ ] Refactor engine/parser.py into Independent Extension Parsing Pipelines:
    - JSON Parser: Native structural load via dictionary mapping (Completed).
    - MD Parser: Specialized check for markdown headers and code fence block skips.
    - TXT Parser: Lightweight, lightning-fast regex splitting without code fence tracking overhead.
    - PDF Parser: Sanitize line fragmentation caused by page extraction coordinates before token split validation.
[ ] Update `parse_lm_studio_file` regex in engine/parser.py to evaluate markdown code block fences properly, ensuring literal string examples of `<think>` inside code blocks are ignored and not purged.

TODO: Robustness, Memory Safety & Parsing Recovery Trackers
---------------------------------------------------------------------------------
[ ] Integrate Robust Delimiter-Based Parsing / Schema-Guided Recovery Layer:
    - Upgrade the JSON repair logic in engine/compression.py to utilize a robust parser strategy (e.g., a utility like robust-json).
    - Ensure extraction can safely ignore loose model comments, handle trailing object commas, and recover usable schemas from partially truncated strings.
[ ] Implement Multi-Threaded Thread Safety & QThread Lifecycle Protections:
    - Audit background workflow lifecycles inside gui/main_window.py.
    - Standardize thread shutdown routes to explicitly enforce the Quit-Wait execution pattern (worker.quit() followed by worker.wait()) to eliminate testing/production resource leaks.
[ ] Update engine initialization in database/connection.py to include a busy timeout configuration (`connect_args={"check_same_thread": False, "timeout": 30}`) to mitigate multi-threaded `OperationalError: database is locked` write contentions during simultaneous background operations.
[ ] Update worker spawning loop in gui/main_window.py to explicitly connect the worker thread's `finished` signal to its own `deleteLater` slot, ensuring safe object deletion upon pipeline completion.